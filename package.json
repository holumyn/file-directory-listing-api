{
  "name": "directory-listing",
  "version": "1.0.0",
  "description": "Directory Listing API Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha --recursive --exit --timeout 90000 ./test/**/*.js",
    "coverage": "nyc --reporter=html mocha --recursive --exit --timeout 90000 ./test/**/*.js"
  },
  "author": "Olumide Oduola",
  "license": "ISC",
  "dependencies": {
    "app-root-path": "^3.0.0",
    "body-parser": "^1.19.0",
    "dotenv-flow": "^3.2.0",
    "express": "^4.17.1",
    "proxyquire": "^2.1.3",
    "sinon": "^9.2.4"
  },
  "devDependencies": {
    "mocha": "^8.2.1",
    "nyc": "^15.1.0"
  }
}
